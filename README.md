# ⚠️ DEPRECATED — moved

**This repo was consolidated into `claude-social-media-skills` on 2026-05-30.**

The entire tree (Go CLI, the `yt-analytics` / `yt-launch` / `yt-ab` / `discord-invite`
skills, data, launchd agents, growth-priorities doc) now lives at:

> **`~/dev/claude-social-media-skills/youtube-analytics/`**
> (`github.com/michaellady/claude-social-media-skills`)

Everything social now lives in one repo, with **`/flywheel`** as the single pane of
glass over the whole closed loop. The `clip-results` skill was retired — its
cross-platform browser-scrape method now lives in `/opus-clips-performance` (Phase 3),
and `_shared/content-attribution` is the JOIN engine.

**Do new work in the consolidated repo, not here.** This directory is kept only as a
fallback snapshot and may be deleted once the migration is confirmed stable.
